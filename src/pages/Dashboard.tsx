import React from 'react'
import { TotalitaPage } from '../features/dashboard/TotalitaPage'

export default function Dashboard() {
  // this page simply exposes the main dashboard feature; layout and nav are handled globally
  return <TotalitaPage />
}
